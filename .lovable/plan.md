# Remaining Build Plan\n\nAlready completed (skipped here): glassmorphism + color tokens, GlobalNav with About/ROP, Discover + profiles + awards parsing/gating, Exit vs End semantics, 48h archive banner on public page, committee_type field, SecGen observer link, realtime publication for pois/blocs/agendas/updates/delegates, profiles RLS, parse-awards function.\n\n## 1. Stability & Error Fixes (do first)\n- **Standalone committee chair name error**: in `StandaloneChairPortal.tsx` and `CreateStandaloneCommittee.tsx`, ensure committee row is committed (await + check `data`) before any `chair_sessions` insert; trim displayName, enforce 2–60 chars, reject if committee_id missing.\n- **Auth flow**: in `Auth.tsx`, surface specific Supabase errors, ensure profile row is created on first sign-in (upsert by user_id) so Profile/Discover never 404.\n- **Committee setup**: in `SecGenDashboard` committee creation, validate name + type before insert; generate chair_code via `generate_code(8)` server-side default if null.\n- Add zod validation on all create forms (conference, committee, standalone, profile, delegate join).\n\n## 2. Committee Initialization Gate\n- New `ChairSetupGate` component shown in `ChairPortal` / `StandaloneChairPortal` until:\n  1. At least one row in `committee_agendas` (required).\n  2. `delegations` (country matrix) on committee is non-empty.\n  3. Optional: at least one `committee_files` row.\n- Until satisfied, block delegate join: delegate join page reads committee and shows \